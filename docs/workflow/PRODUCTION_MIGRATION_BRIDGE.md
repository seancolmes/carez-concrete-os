# Production Migration Bridge — Issue #59

## Purpose and authority

This manifest reconciles the accepted QA migration chain with the existing production schema and records the completed Issue #59 bridge application. Source migrations define the desired contract; read-only production schema and migration history define the comparison baseline. This document authorizes no provider mutation.

Before the Issue #59 application, read-only production migration history ended at `20260831075000_method_profile_commit_fix`. The completed bridge is recorded below using the application-time versions created by successful migration application; canonical source filenames remain the manifest authority.

**ISSUE #59 PRODUCTION BRIDGE: APPLIED AND VERIFIED.** The production schema-only dump was restored into a separate ACL-faithful local Supabase target, all 34 `SAFE_TO_APPLY` migrations passed in source order, the scoped production row-dependent preflight passed, and the specifically authorized production application completed successfully. Production migration history records application-time versions `20260925021524` through `20260925022046`; the 34 canonical migration names match the manifest entries 1:1 and in source order. `20260924083100_proposal_authority_cutover` was applied last. No production data incompatibility or remediation requirement was identified. This documentation update authorizes no further production write; production write authorization is **NOT GRANTED** by this record.

Production project ref: `snbnwgetfuvkjkhfxmmz`
QA project ref: `tkcirsdfvvahwrcratkn`

**Never run a blind `supabase db push` against production.** Issue #59 completion does not authorize later production mutations. Rehearse only against an isolated local clone built from a production schema-only dump. Never include production row data.

## Classifications

- **ALREADY_MATERIALIZED** — production already has the equivalent current contract; do not replay the historical migration blindly.
- **SAFE_TO_APPLY** — apply in source order only after successful production-schema clone rehearsal and the listed prerequisites/collision review.
- **BRIDGE_ONLY** — QA bootstrap or compatibility history that must not be replayed against production as an ordinary migration.
- **NOT_APPLICABLE** — removed, superseded, or outside the accepted production contract; skip it.

## Migration mapping

| Source migration / contract | Classification | Production handling, prerequisites, and known collision risk |
| --- | --- | --- |
| `takeoff_scale_regions` (`20260831012827_takeoff_scale_regions.sql`) | ALREADY_MATERIALIZED | This is a contract label, not a literal PostgreSQL object. Final mapping: `public.takeoff_scale_regions`; `takeoff_sheets_scale_status_check`; `takeoff_measurements.scale_region_id`, `takeoff_measurements_company_scale_region_fk`, and `takeoff_measurements_scale_region_idx`; relation constraints `takeoff_scale_regions_company_id_id_key`, `takeoff_scale_regions_company_sheet_id_key`, `takeoff_scale_regions_company_sheet_fk`, `takeoff_scale_regions_confidence_check`, `takeoff_scale_regions_calibration_check`, and `takeoff_scale_regions_bounds_check`; indexes `takeoff_scale_regions_default_uq`, `takeoff_scale_regions_sheet_idx`, and `takeoff_scale_regions_set_idx`; trigger `carez_takeoff_scale_regions_touch` using `carez_takeoff_touch_updated_at()`; policies `office can view takeoff scale regions`, `office can insert takeoff scale regions`, `office can update takeoff scale regions`, and `office can delete takeoff scale regions`; and `carez_geometry_within_scale_bounds(jsonb,jsonb)`, `carez_upsert_takeoff_scale_region(uuid,uuid,text,jsonb,text,text,text,text,jsonb,numeric,jsonb,boolean)`, `carez_delete_takeoff_scale_region(uuid)`, and `carez_save_takeoff_sheet_calibration(uuid,numeric,numeric,jsonb)`. `carez_update_drawing_measurement(uuid,jsonb,numeric,text,jsonb,jsonb,uuid)` is represented by the later Condition-invalidation definition in `20260907004754`; use that final body. The drawing-commit RPC is represented by the final method-profile definition in `20260831044640`, not its earlier scale-only body. |
| `estimating_workstation_section_assignment` (`20260831032746_estimating_workstation_section_assignment.sql`) | ALREADY_MATERIALIZED | This is a contract label, not a relation. Its material RPC is `public.carez_assign_takeoff_measurement_section(uuid,uuid)`. |
| `estimating_summary_view_contract` (`20260831034451_estimating_summary_view_contract.sql`) | ALREADY_MATERIALIZED | This is a contract label, not a relation. Its objects are `public.estimate_financial_summary` and `public.estimate_takeoff_summary`, both `security_invoker=true`. Financial columns: `estimate_id`, `company_id`, `estimate_number`, `name`, `status`, `version`, `project_id`, `target_margin_percent`, `bo_classification`, `bo_rate_percent`, `payment_processing_rate_percent`, `overhead_rate_snapshot`, `proposed_sell_price`, `labor_hours`, `direct_labor_cost`, `material_cost`, `equipment_cost`, `subcontractor_cost`, `other_direct_cost`, `total_direct_cost`, `overhead_cost`, `base_company_cost`, `recommended_sell_price`, `selected_sell_price`, `revenue_cost_reserve`, `projected_profit`, and `projected_margin_percent`. Costs aggregate tenant-matched estimate items; sell-price formulas apply overhead and margin/BO/payment-processing rates and use a positive proposed sell price when present. Takeoff columns: `company_id`, `estimate_id`, `active_measurements`, `generated_outputs`, `missing_price_outputs`, `takeoff_man_hours`, and `takeoff_direct_cost`. Both views grant SELECT to `anon`, `authenticated`, and `service_role`. |
| `builder_method_profiles` (`20260831042701_builder_method_profiles.sql`) | ALREADY_MATERIALIZED | This is a contract label, not one relation. It maps to `concrete_assembly_variables.input_role`, `concrete_assembly_variables.requires_verification`, and `concrete_assembly_variables_input_role_check`; `public.takeoff_method_profiles` columns `company_id`, `takeoff_set_id`, `assembly_version_id`, `revision_no`, `name`, `status`, `method_inputs`, `verification_notes`, `verified_by`, `verified_at`, `created_by`, `created_at`, and `updated_at` (with positive revision, nonblank name, allowed-status, and object-input checks); `takeoff_method_profiles_set_idx`; `takeoff_measurements.method_profile_id`, `takeoff_measurements_method_profile_company_fkey`, and `takeoff_measurements_method_profile_idx`; triggers `takeoff_method_profiles_guard`/`takeoff_method_profiles_touch` using `carez_guard_takeoff_method_profile()`/`carez_touch_takeoff_method_profile()`; policies `takeoff_method_profiles_select`, `takeoff_method_profiles_insert`, `takeoff_method_profiles_update`, and `takeoff_method_profiles_delete`; `carez_verify_takeoff_method_profile(uuid)`; and `carez_create_assembly_revision(uuid)`. |
| `method_profile_activation_validation` (`20260831042922_method_profile_activation_validation.sql`) | ALREADY_MATERIALIZED | This is a contract label, not one relation. Its helpers are `carez_method_rule_value(jsonb,jsonb)` and `carez_method_activation_matches(jsonb,jsonb)`; the final verification RPC is `carez_verify_takeoff_method_profile(uuid)`. |
| `method_profile_measurement_lineage` (`20260831043107_method_profile_measurement_lineage.sql`) | ALREADY_MATERIALIZED | The intermediate validator in `20260831043107` is superseded. The final `carez_validate_measurement_method_profile()` trigger function is attached by `takeoff_measurements_method_profile_guard` before INSERT or UPDATE OF `method_profile_id,takeoff_set_id,assembly_version_id,variables`; it validates verified profile lineage and active builder assumptions against measurement variables. Use the later `20260831044640` definition, including its guarded unbound-profile setting behavior. |
| `method_profile_commit_contract` (`20260831043530_method_profile_commit_contract.sql`) | ALREADY_MATERIALIZED | `carez_create_verified_takeoff_method_profile(uuid,uuid,text,jsonb,text)` remains materially represented. Its intermediate `carez_commit_drawing_measurement` implementation is superseded by `20260831044640`; do not treat the intermediate body as the production contract. |
| `method_profile_commit_fix` (`20260831044640_method_profile_commit_fix.sql`) | ALREADY_MATERIALIZED | Final mapping: `carez_validate_measurement_method_profile()` and `carez_commit_drawing_measurement(uuid,uuid,uuid,uuid,text,text,text,text,numeric,text,jsonb,text,jsonb,jsonb,uuid,uuid)`. The final commit checks verified-profile lineage and active assumptions; it sets `carez.allow_unbound_method_profile_insert` only around the nested legacy commit when a profile is required, then resets it before the method-profile lineage update. |
| `20260830235252_qa_bootstrap_pre_migration_core` | BRIDGE_ONLY | QA bootstrap baseline; never replay as a production migration. |
| `20260830235541_qa_takeoff_contract_tables` | BRIDGE_ONLY | QA Takeoff bootstrap; production has historical equivalents listed above. |
| `20260830235636_qa_takeoff_contract_rpcs` | BRIDGE_ONLY | QA Takeoff RPC bootstrap; reconcile against production contract rather than replaying. |
| `20260901044057_takeoff_nested_activation_missing_input` | SAFE_TO_APPLY | Replaces `carez_sync_takeoff_measurement_outputs` so inactive nested branches preserve `missing_input`; requires Takeoff assembly/output schema. Compare function signature/body and dependent columns. |
| `20260903040801_assembly_builder_revision_clone` | SAFE_TO_APPLY | Defines `carez_create_assembly_revision(uuid)` and clones version variables, components, children, and property bindings; requires assembly schema. Compare exact function identity and referenced columns. |
| `20260903044953_qa_reconcile_custom_assembly_create_rpc` | SAFE_TO_APPLY | Defines `carez_create_custom_assembly(text,text,text,text,uuid,text)`; requires assembly/folder schema and grants. Check overload and function-definition collision. |
| `20260903052242_scope_recipe_project_variants` | SAFE_TO_APPLY | Adds profile-kind/variant support, guard/validation behavior, and verified scope-variant creation; requires method-profile tables and dependencies. Compare constraints, indexes, triggers, and function signatures. |
| `20260903061837_formula_composer_authoring` | SAFE_TO_APPLY | Extends assembly component authoring and defines assembly revision cloning; requires formula schema. Replaces a function from `20260903040801`; apply in source order and compare final body. |
| `20260904054917_concrete_condition_foundation` | SAFE_TO_APPLY | Prerequisite for current Condition chain. Compare existing relations, enums, triggers, and policies in the production-schema clone. |
| `20260904055348_condition_foreign_key_indexes` | SAFE_TO_APPLY | Requires foundation; compare existing constraints/indexes before replay. |
| `20260904135826_condition_persistence_reconciliation` | SAFE_TO_APPLY | Requires preceding Condition chain; collision risk with historical persistence objects. |
| `20260904210858_company_branding` | SAFE_TO_APPLY | Creates tenant branding contract, storage bucket/policies, and purchase-order logo snapshot support; requires Storage and purchase-order schema. Check bucket/policy/column/function collisions. |
| `20260905001253_condition_pilot_template_bootstrap` | SAFE_TO_APPLY | Requires Condition schema; inspect existing template objects and version collisions in the clone. |
| `20260905155018_strip_footing_edge_modules_v2` | SAFE_TO_APPLY | Adds Condition module validation and publishes Strip v2 archetype/template behavior; requires Condition foundation and v1. Includes archetype data insertion; check unique version collisions in clone. |
| `20260905162324_condition_draft_deletion` | SAFE_TO_APPLY | Requires preceding Condition chain; review function/policy name collisions. |
| `20260905205715_strip_footing_estimator_model_v3` | SAFE_TO_APPLY | Publishes Strip v3 and ensures its company template/projection; requires Strip v2 and Condition foundation. Inserts archetype data; check version uniqueness and function dependencies. |
| `20260906030428_condition_commercial_reconciliation_and_contract_upgrade` | SAFE_TO_APPLY | Requires preceding Condition chain; compare upgraded contract and dependent objects. |
| `20260906054504_condition_repeatable_module_commit` | SAFE_TO_APPLY | Requires current Condition contract; compare existing RPC signatures and grants. |
| `20260906150633_strip_footing_bulkheads_v4` | SAFE_TO_APPLY | Publishes Strip v4 bulkhead contract and draft upgrade functions; requires v3/current Condition schema. Inserts archetype data; preserve published history and check version collisions. |
| `20260906150800_strip_footing_bulkheads_v4_upgrade_order` | SAFE_TO_APPLY | Replaces v4 draft upgrade function so contract retarget precedes v4-only inputs; requires v4 migration. Compare exact function body. |
| `20260906160338_strip_footing_form_resources_v5` | SAFE_TO_APPLY | Publishes physical form-board v5 and v5 template/draft upgrade behavior; requires published v4. Inserts archetype data; check unique version collisions in clone. |
| `20260906175305_reconcile_identity_helper_rpc_lockdown` | ALREADY_MATERIALIZED | Production dump ACLs for `public.get_my_company_id()` and `public.get_my_role()` match the lockdown: stable SQL `SECURITY DEFINER`, `search_path=public`, production-compatible owner, no PUBLIC/anon execution, and EXECUTE for authenticated/service_role. Do not reclassify as `SAFE_TO_APPLY`. |
| `20260907004754_condition_measurement_invalidation` | SAFE_TO_APPLY | Requires current Condition contract; compare trigger/function names and dependencies. |
| `20260915231000_condition_legacy_migration_ledger` | SAFE_TO_APPLY | Requires Condition chain; check legacy ledger object collision. |
| `20260915232000_condition_legacy_migration_commit` | SAFE_TO_APPLY | Requires ledger migration; compare commit RPC signatures and grants. |
| `20260920183000_restore_projects_source_estimate_lineage` | ALREADY_MATERIALIZED | Verified current contract; `projects.source_estimate_id` is materially present. |
| `20260921210000_estimate_pricing_provenance` | SAFE_TO_APPLY | Requires current Condition chain; compare existing Estimate pricing columns/constraints. |
| `20260921211000_estimate_pricing_provenance_indexes` | SAFE_TO_APPLY | Requires pricing provenance; inspect index names/definitions. |
| `20260921224000_estimate_supplier_quotes` | SAFE_TO_APPLY | Requires P1 pricing provenance; inspect supplier quote relations and RLS. |
| `20260921224500_estimate_supplier_quote_indexes` | SAFE_TO_APPLY | Requires supplier quote migration; inspect index names/definitions. |
| `20260921231000_estimate_labor_production_overrides` | SAFE_TO_APPLY | Requires P1 pricing provenance; inspect labor relations, constraints, and policies. |
| `20260921231500_labor_rate_selection_sticky_marker` | SAFE_TO_APPLY | Requires labor migration; inspect marker columns and functions. |
| `20260921232000_estimate_review_acknowledgements` | SAFE_TO_APPLY | Requires pricing/labor contract; check acknowledgement object collisions and immutability. |
| `20260923120000_reconcile_proposal_conversion_schema` | NOT_APPLICABLE | Removed from canonical QA chain because neither QA nor production applied it. Do not restore its historical Acceptance/Award/Revision behavior. |
| `20260923120500_reconcile_company_billing_profiles` | ALREADY_MATERIALIZED | Verified current billing profile contract is materially present. |
| `20260923121000_estimate_release_readiness` | SAFE_TO_APPLY | Requires pricing, supplier quote, labor, and review acknowledgement migrations; readiness evaluator remains authority. |
| `20260923122000_estimate_review_acknowledgement_rpc` | SAFE_TO_APPLY | Requires immutable acknowledgement schema and readiness evaluator; compare exact RPC signature/grants. |
| `20260923122500_proposal_release_guard` | SAFE_TO_APPLY | Requires readiness evaluator and acknowledgement RPC; `guard_proposal_release` is Proposal release authority. |
| `20260923123000_estimate_review_acknowledgement_hardening` | SAFE_TO_APPLY | Requires acknowledgement schema/RPC and release guard; verify immutable fingerprint and acknowledgement lineage. |
| `20260924083000_reconcile_proposal_conversion_schema` | SAFE_TO_APPLY | Requires Condition, P1 pricing, supplier quote, labor, review acknowledgement, readiness, acknowledgement RPC, Proposal guard, and hardening migrations. Production has historical Proposal objects; inspect collisions and preserve snapshots. |
| `20260924083100_proposal_authority_cutover` | SAFE_TO_APPLY — bridge last | Requires `20260924083000` and its P1.4 trigger. Fails closed if guard is absent, conditionally drops only `carez_proposal_audit_gate`, and revokes app-role execution from exact historical signatures. Apply last; compare existing trigger and function ACLs first. |

## Proposal authority and deferred capabilities

The canonical accepted Proposal reconciliation is `20260924083000_reconcile_proposal_conversion_schema`. Preserve `carez_get_estimate_release_readiness` as readiness authority and `guard_proposal_release` as Proposal release authority, together with immutable commercial/warning fingerprints and acknowledgement lineage.

Production preflight reports historical `carez_proposal_audit_gate` (BEFORE INSERT on `public.proposal_presentations`, executing `carez_guard_proposal_audit()`) and exact historical Acceptance/Award/Revision signatures, while QA may not carry those objects. The cutover verifies that `guard_proposal_release` on `public.proposal_presentations` invokes `public.carez_guard_proposal_release()` and raises before any cutover if absent. Only after verification does it conditionally drop the historical trigger and revoke EXECUTE from `public`, `anon`, and `authenticated` on each existing historical function. It does not change `service_role` ACLs, drop/redefine/execute those functions, or mutate Proposal/Estimate/Project rows. It is safe when the historical trigger/functions are absent.

**Customer Acceptance and Award remain deferred. Create Next Revision (`create_estimate_revision`) remains deferred.** Do not restore these capabilities, execute Acceptance/Award, create Award/Project records, or execute Create Next Revision.

## Rehearsal gate

Production schema-only clone rehearsal validates DDL, schema, function, trigger, RLS, ACL, and migration-order compatibility; it does **not** prove compatibility with existing production rows. A separate read-only production data preflight was completed for the row-dependent branding, pricing provenance, and Proposal opportunity-number behavior below. Do not copy production row data into the local clone unless separately authorized.

## Production row-dependent preflight — read-only

The preflight used aggregate counts, catalog facts, and the one required bucket configuration lookup. No production rows were copied or modified.

- `20260904210858_company_branding`: the `carez-branding` bucket is absent. The migration creates it with `public=true`, `file_size_limit=5242880`, and allowed MIME types `image/png`, `image/jpeg`, and `image/webp`. No existing bucket configuration is overwritten. **PASS.**
- `20260921210000_estimate_pricing_provenance`: all seven pricing provenance columns are absent from both `takeoff_measurement_outputs` and `estimate_items`. The 26 Takeoff outputs comprise 12 `priced`, 0 `manual_override`, 10 `missing_price`, and 4 `not_priced`. All 12 priced outputs have nonblank trimmed `cost_source`; the migration assigns each `price_source_kind='unknown'` and `price_source_label=trimmed cost_source`. Each has exactly one linked Estimate item; all 12 items receive the output's seven newly added provenance fields through the `company_id` and `source_takeoff_output_id` match. There are no existing provenance-column values to overwrite and no lineage gap or multiplicity conflict. The manual-override backfill affects zero rows. **EXPECTED LEGACY BACKFILL — PASS.**
- `20260924083000_reconcile_proposal_conversion_schema`: `leads.opportunity_number` is nullable `text` with no default. The intended `leads_company_opportunity_number_key` partial unique index already exists on `(company_id, opportunity_number)` where `opportunity_number IS NOT NULL`. One non-null value and zero duplicate groups were found. **PASS.**

Overall production row preflight: **PASS**. Production schema/data incompatibility: **NONE IDENTIFIED**. Production remediation required before bridge application: **NO**. Production write authorization: **NOT GRANTED**.

**Production bridge acceptance requires a successful ACL-faithful production-schema clone rehearsal.** An ordinary fresh local Supabase target may already have `pg_default_acl` entries scoped to `public` that grant privileges to `anon`, `authenticated`, and `service_role` when restored objects are created. In the verified restore case, the dump correctly revoked `PUBLIC`, but that did not remove a separate explicit `anon` grant injected by the target's object-creation defaults. Therefore effective privileges from an ordinary local restore are not automatically production ACL evidence. This caveat is limited to the verified local default-privilege behavior; use the production schema dump and an ACL-faithful restore as ACL authority.

The accepted schema-only artifact used for this verification was `C:\Users\nikca\Documents\Carez-Issue59-Rehearsal\dump\production-schema.sql`, SHA-256 `8760E07168FB94A1CA7FD144A10FDD628138C1AE76145E2E6D73006CBFFD314A`. It was schema-only, had no business-row `COPY`/`INSERT` payload, remained unchanged during restore, and restored successfully into PostgreSQL 17.6. No production or QA access was used for this local verification.

Before restoring production objects into a separate isolated local Supabase target:

1. Capture the target's initial `pg_default_acl` state and identify entries scoped to `public` for the role that will own restored objects.
2. In that isolated **local target only**, neutralize that owner's default grants to `anon`, `authenticated`, and `service_role` for tables, sequences, and functions in `public` before creating/restoring production objects.
3. Do not alter production, QA, role memberships, required Supabase platform schemas, or unrelated `storage` defaults.
4. Restore the unchanged production schema-only dump. Do not hand-edit or rewrite it. Allow its trailing `ALTER DEFAULT PRIVILEGES` statements to establish the dump's intended final defaults.
5. Compare actual object ACL structures to the ACL statements serialized in the production dump, including owner and role grants. Do not declare fidelity based only on `has_function_privilege` or `has_table_privilege`.
6. Verify RLS flags and policies independently from ACLs.

This procedure produced an ACL-faithful clone for the sampled Issue #59 objects. The accepted local target identity was `supabase_db_Carez-Issue59-ACL-Faithful` under `C:\Users\nikca\Documents\Carez-Issue59-Rehearsal\ACL-Faithful`. The earlier `supabase_db_Carez-Issue59-Rehearsal` clone remains diagnostic evidence only and is not authoritative for ACL verification. Do not place dump files, credentials, or disposable database state in source control.

The completed rehearsal used the ACL-faithful clone, applied only `SAFE_TO_APPLY` migrations in source order, and applied `20260924083100_proposal_authority_cutover` last. All 34 migrations passed. Historical production-only schema, `projects.source_estimate_id`, identity helper ACLs, current Condition/P1 schema, immutable review acknowledgements, readiness and acknowledgement RPCs, Proposal release evidence, the single `guard_proposal_release` INSERT authority, historical trigger removal, deferred function ACLs, and public Proposal view/question-response functions were verified. No production or QA writes occurred, and no production business rows were copied.

Current status: **ACL-FAITHFUL CLONE PASS / 34-MIGRATION SCHEMA REHEARSAL PASS / PRODUCTION ROW PREFLIGHT PASS / 34-OF-34 PRODUCTION MIGRATIONS APPLIED / PROPOSAL AUTHORITY CUTOVER LAST / PRODUCTION WRITE AUTHORIZATION NOT GRANTED BY THIS DOCUMENTATION UPDATE.**

## Production bridge application — completed

The authorized Issue #59 bridge was applied successfully: all 34 `SAFE_TO_APPLY` migrations completed in canonical source order. Production migration-history versions were assigned at application time from `20260925021524` through `20260925022046`; canonical migration names match the manifest 1:1. No manual migration-history repair was performed. The `20260924083100_proposal_authority_cutover` migration ran last.

Post-apply verification passed: `guard_proposal_release` is the sole Proposal INSERT release authority; the historical `carez_proposal_audit_gate` is absent; the historical Acceptance/Award/Revision functions remain defined but application execution is revoked for `PUBLIC`, `anon`, and `authenticated`, with `service_role` treatment preserved. Readiness and acknowledgement authority, identity-helper ACLs, relevant RLS/policies, and public Proposal view/question-response capabilities remain intact. Customer Acceptance, Award → Project, and Create Next Revision remain deferred.

Expected data effects passed: the `carez-branding` bucket was configured as intended, and pricing provenance was backfilled for 12 priced Takeoff outputs and their 12 one-to-one linked Estimate items. Proposal reconciliation passed. No unrelated production mutation was identified. Issue #59 is no longer a production-release blocker for this migration bridge; any later production write requires its own explicit authorization.
