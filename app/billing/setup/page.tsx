import {redirect} from 'next/navigation';
import Link from 'next/link';
import {AppShell} from '@/components/AppShell';
import {createClient} from '@/lib/supabase/server';
import {updateCompanyBillingProfile,updateCustomerBillingProfile,updateProjectBillingSetup} from '../actions';
const num=(n:any)=>Number(n||0);
export default async function BillingSetupPage(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login');const {data:p}=await supabase.from('profiles').select('full_name,company_id,role').eq('id',user.id).single();if(!p?.company_id)redirect('/login');if(p.role==='employee')redirect('/employee');const [{data:bp},{data:projects}]=await Promise.all([supabase.from('company_billing_profiles').select('*').eq('company_id',p.company_id).maybeSingle(),supabase.from('projects').select('id,job_number,name,sales_tax_rate_percent,sales_tax_exempt,sales_tax_jurisdiction,customers(id,name,contact_name,email,phone,billing_terms,billing_address_line1,billing_address_line2,billing_city,billing_state,billing_postal_code)').in('status',['active','on_hold','completed']).order('job_number')]);return <AppShell userName={p.full_name||user.email||'Owner'}><div className="contractor-page"><div className="command-hero"><div><h1>Billing Setup</h1><p>Company invoice information, customer bill-to information and job sales-tax setup. This is setup data, not daily bookkeeping.</p></div><div className="command-actions"><Link className="button secondary" href="/billing">Back to Get Paid</Link></div></div><section className="section"><div className="surface"><div className="surface-header"><div><div className="surface-title">Carez Invoice Information</div><div className="surface-subtitle">What customers see at the top and bottom of Carez invoices.</div></div></div><div className="surface-body"><form action={updateCompanyBillingProfile} className="form"><div className="grid grid2"><label className="field"><span>Business Name</span><input name="display_name" defaultValue={bp?.display_name||'Carez Concrete'} required/></label><label className="field"><span>Legal Name</span><input name="legal_name" defaultValue={bp?.legal_name||''}/></label></div><label className="field"><span>Business Address</span><input name="address_line1" defaultValue={bp?.address_line1||''}/></label><div className="grid grid2"><label className="field"><span>City</span><input name="city" defaultValue={bp?.city||''}/></label><label className="field"><span>State</span><input name="state" defaultValue={bp?.state||'WA'}/></label></div><div className="grid grid2"><label className="field"><span>ZIP</span><input name="postal_code" defaultValue={bp?.postal_code||''}/></label><label className="field"><span>Phone</span><input name="phone" defaultValue={bp?.phone||''}/></label></div><div className="grid grid2"><label className="field"><span>Billing Email</span><input type="email" name="email" defaultValue={bp?.email||''}/></label><label className="field"><span>Website</span><input name="website" defaultValue={bp?.website||''}/></label></div><div className="grid grid2"><label className="field"><span>WA UBI</span><input name="ubi_number" defaultValue={bp?.ubi_number||''}/></label><label className="field"><span>Contractor License</span><input name="contractor_license_number" defaultValue={bp?.contractor_license_number||''}/></label></div><div className="grid grid2"><label className="field"><span>Default Days to Pay</span><input type="number" name="default_due_days" min="0" defaultValue={num(bp?.default_due_days)}/></label><label className="field"><span>Default Terms</span><input name="default_terms_text" defaultValue={bp?.default_terms_text||''} placeholder="Due on receipt / Net 15"/></label></div><label className="field"><span>How Customers Pay Us</span><textarea rows={3} name="payment_instructions" defaultValue={bp?.payment_instructions||''}/></label><label className="field"><span>Invoice Footer</span><textarea rows={2} name="invoice_footer" defaultValue={bp?.invoice_footer||''}/></label><button className="button">Save Carez Invoice Setup</button></form></div></div></section><section className="section"><div className="section-heading"><div><div className="section-kicker">Jobs</div><div className="section-title">Customer & Sales Tax Setup</div><div className="section-heading-meta">Set these once per job so invoices calculate correctly.</div></div></div><div className="project-list">{(projects||[]).map((x:any)=>{const c:any=x.customers;return <article className="project-card" key={x.id}><header className="project-header"><div><div className="project-name">{x.job_number} — {x.name}</div><div className="project-location">{c?.name||'Customer not linked'}</div></div></header><section className="project-section"><div className="grid grid2"><form action={updateProjectBillingSetup} className="form"><input type="hidden" name="project_id" value={x.id}/><div className="title">Job Sales Tax</div><label className="field"><span>Tax Rate %</span><input type="number" min="0" step="0.001" name="sales_tax_rate_percent" defaultValue={num(x.sales_tax_rate_percent)}/></label><label className="field"><span>Jurisdiction</span><input name="sales_tax_jurisdiction" defaultValue={x.sales_tax_jurisdiction||''}/></label><label className="meta"><input type="checkbox" name="sales_tax_exempt" defaultChecked={Boolean(x.sales_tax_exempt)}/> Tax-exempt job/customer</label><button className="button secondary">Save Tax Setup</button></form>{c?<form action={updateCustomerBillingProfile} className="form"><input type="hidden" name="customer_id" value={c.id}/><div className="title">Where to Send the Bill</div><label className="field"><span>Contact</span><input name="contact_name" defaultValue={c.contact_name||''}/></label><div className="grid grid2"><label className="field"><span>Email</span><input type="email" name="email" defaultValue={c.email||''}/></label><label className="field"><span>Phone</span><input name="phone" defaultValue={c.phone||''}/></label></div><label className="field"><span>Billing Address</span><input name="billing_address_line1" defaultValue={c.billing_address_line1||''}/></label><div className="grid grid2"><label className="field"><span>City</span><input name="billing_city" defaultValue={c.billing_city||''}/></label><label className="field"><span>State</span><input name="billing_state" defaultValue={c.billing_state||'WA'}/></label></div><div className="grid grid2"><label className="field"><span>ZIP</span><input name="billing_postal_code" defaultValue={c.billing_postal_code||''}/></label><label className="field"><span>Terms</span><input name="billing_terms" defaultValue={c.billing_terms||''}/></label></div><button className="button secondary">Save Customer Billing</button></form>:<div className="alert warn">Link this project to a customer before sending invoices.</div>}</div></section></article>})}</div></section></div></AppShell>}
