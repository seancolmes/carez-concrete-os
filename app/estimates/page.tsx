import {redirect} from 'next/navigation';
import Link from 'next/link';
import {AppShell} from '@/components/AppShell';
import {createClient} from '@/lib/supabase/server';
import {createEstimate,addEstimateSection,addEstimateItem,updateEstimatePricing,approveEstimateToBudget} from './actions';
const money=(n:any)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(Number(n||0));const num=(n:any)=>Number(n||0);const units=['CY','LF','SF','LB','EA','HR','DAY','TON','GAL','LS'];
export default async function EstimatesPage(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect('/login');const {data:p}=await supabase.from('profiles').select('full_name,company_id,role').eq('id',user.id).single();if(!p?.company_id)redirect('/login');if(p.role==='employee')redirect('/employee');const [{data:projects},{data:estimates},{data:summaries},{data:sections},{data:items},{data:codes},{data:catalog},{data:crew},{data:risk},{data:budgets}]=await Promise.all([supabase.from('projects').select('id,job_number,name,status').eq('company_id',p.company_id).order('created_at',{ascending:false}),supabase.from('estimates').select('*').eq('company_id',p.company_id).order('created_at',{ascending:false}),supabase.from('estimate_financial_summary').select('*'),supabase.from('estimate_sections').select('*').eq('company_id',p.company_id).order('sort_order'),supabase.from('estimate_items').select('*').eq('company_id',p.company_id).order('sort_order'),supabase.from('cost_codes').select('*').eq('company_id',p.company_id).eq('active',true).order('sort_order'),supabase.from('cost_catalog_items').select('id,name,cost_code_id,default_unit').eq('company_id',p.company_id).eq('active',true).order('name'),supabase.from('crew_members').select('id,name,hourly_rate,internal_field_rate,is_owner').eq('company_id',p.company_id).eq('active',true).order('name'),supabase.from('li_risk_classes').select('code,name').eq('company_id',p.company_id).eq('tax_year',2026).eq('active',true).order('code'),supabase.from('project_budgets').select('project_id,id,label,status').eq('company_id',p.company_id).eq('status','active')]);const sMap=new Map((summaries||[]).map((x:any)=>[x.estimate_id,x])),projectMap=new Map((projects||[]).map((x:any)=>[x.id,x])),sectionsBy=new Map<string,any[]>(),itemsBy=new Map<string,any[]>();for(const x of sections||[]){const a=sectionsBy.get(x.estimate_id)||[];a.push(x);sectionsBy.set(x.estimate_id,a);}for(const x of items||[]){const a=itemsBy.get(x.estimate_id)||[];a.push(x);itemsBy.set(x.estimate_id,a);}const budgeted=new Set((budgets||[]).map((x:any)=>x.project_id)),open=(estimates||[]).filter((e:any)=>!['approved','declined'].includes(e.status)),ready=open.filter((e:any)=>['ready','accepted'].includes(e.status)),totalPipeline=open.reduce((s:number,e:any)=>s+num(sMap.get(e.id)?.recommended_sell_price),0);return <AppShell userName={p.full_name||user.email||'Owner'}><div className="contractor-page"><div className="command-hero"><div><h1>Estimates</h1><p>Build the concrete price from real labor, material, equipment and subcontract costs. When the job is won, freeze the estimate into the project budget.</p></div><div className="command-actions"><Link className="button secondary" href="/proposals">Customer Proposals</Link></div></div><div className="command-grid"><div className="command-card"><div className="command-label">Open Estimates</div><div className="command-value">{open.length}</div></div><div className={`command-card ${ready.length?'watch':''}`}><div className="command-label">Ready / Accepted</div><div className="command-value">{ready.length}</div><div className="command-help">Pricing ready for customer or job award.</div></div><div className="command-card"><div className="command-label">Open Pricing Value</div><div className="command-value">{money(totalPipeline)}</div></div></div><section className="section"><div className="surface"><div className="surface-header"><div><div className="surface-title">Start Estimate</div><div className="surface-subtitle">Create it before every cost line is known. Build it up as plans and pricing become available.</div></div></div><div className="surface-body"><form action={createEstimate} className="form"><div className="grid grid2"><label className="field"><span>Estimate Name</span><input name="name" required placeholder="Smith Residence Concrete"/></label><label className="field"><span>Link to Existing Project (optional)</span><select name="project_id" defaultValue=""><option value="">Preconstruction / not a project yet</option>{(projects||[]).map((x:any)=><option key={x.id} value={x.id}>{x.job_number} — {x.name}</option>)}</select></label></div><button className="button">Create Estimate</button></form></div></div></section><section className="section"><div className="section-heading"><div><div className="section-kicker">Pricing</div><div className="section-title">Estimate Board</div><div className="section-heading-meta">Cost first, selling price second. Approved estimates become frozen project budgets and cannot be rewritten by later estimate edits.</div></div></div><div className="project-list">{(estimates||[]).length===0?<div className="empty-state"><div><div className="title">No estimates yet</div></div></div>:(estimates||[]).map((e:any)=>{const s:any=sMap.get(e.id)||{},project:any=projectMap.get(e.project_id),secs=sectionsBy.get(e.id)||[],its=itemsBy.get(e.id)||[],approved=e.status==='approved';return <article className="project-card" key={e.id}><header className="project-header"><div><div className="project-name">{e.estimate_number} — {e.name}</div><div className="project-location">{project?`${project.job_number} — ${project.name}`:'Preconstruction / no project yet'} · Version {e.version}</div></div><span className={`status ${approved?'completed':e.status==='accepted'?'active':e.status==='declined'?'on-hold':''}`}>{e.status}</span></header><section className="project-section"><div className="metric-grid"><div className="metric-card"><div className="label">Direct Cost</div><div className="metric-value">{money(s.total_direct_cost)}</div><div className="metric-detail">Labor + material + equipment + subs</div></div><div className="metric-card"><div className="label">Overhead Recovery</div><div className="metric-value">{money(s.overhead_cost)}</div><div className="metric-detail">{num(s.labor_hours).toFixed(1)} labor hr × {money(e.overhead_rate_snapshot)}/hr</div></div><div className="metric-card brand"><div className="label">Recommended Price</div><div className="metric-value">{money(s.recommended_sell_price)}</div></div><div className={`metric-card ${num(s.projected_margin_percent)<num(e.target_margin_percent)?'warning':'positive'}`}><div className="label">Projected Margin</div><div className="metric-value">{num(s.projected_margin_percent).toFixed(1)}%</div><div className="metric-detail">Projected profit {money(s.projected_profit)}</div></div></div><div className="grid grid2 section"><div className="surface"><div className="surface-header"><div><div className="surface-title">Scope / Assemblies</div><div className="surface-subtitle">Break the job into things a concrete crew actually builds.</div></div></div><div className="surface-body"><div className="list">{secs.length===0?<div className="meta">No scope sections yet.</div>:secs.map((sec:any)=><div className="row" key={sec.id}><div><div className="title">{sec.name}</div><div className="meta">{String(sec.scope_type).replaceAll('_',' ')}</div></div><strong>{money(its.filter((i:any)=>i.section_id===sec.id).reduce((x:number,i:any)=>x+num(i.direct_cost),0))}</strong></div>)}</div>{!approved&&<details className="controls-disclosure section"><summary>Add Scope Area</summary><div className="controls-body"><form action={addEstimateSection} className="form"><input type="hidden" name="estimate_id" value={e.id}/><label className="field"><span>Area / Assembly</span><input name="name" required placeholder="Basement Walls / Garage Slab / Driveway"/></label><label className="field"><span>Concrete Work Type</span><select name="scope_type"><option value="footing">Footing</option><option value="wall">Wall</option><option value="flatwork">Flatwork</option><option value="curb">Curb / Sidewalk</option><option value="repair">Repair</option><option value="other">Other</option></select></label><button className="button secondary">Add Scope</button></form></div></details>}</div></div><div className="surface"><div className="surface-header"><div><div className="surface-title">Selling Price</div><div className="surface-subtitle">Carez calculates a price from cost, overhead, B&O and target margin. Override only when you deliberately choose another price.</div></div></div><div className="surface-body"><form action={updateEstimatePricing} className="form"><input type="hidden" name="estimate_id" value={e.id}/><div className="grid grid2"><label className="field"><span>Target Profit Margin %</span><input name="target_margin_percent" type="number" step="0.1" defaultValue={num(e.target_margin_percent)} disabled={approved}/></label><label className="field"><span>WA B&O Type</span><select name="bo_classification" defaultValue={e.bo_classification} disabled={approved}><option value="retailing">Retailing</option><option value="wholesaling">Wholesaling</option></select></label></div><div className="grid grid2"><label className="field"><span>Card / Processing Reserve %</span><input name="payment_processing_rate_percent" type="number" step="0.01" defaultValue={num(e.payment_processing_rate_percent)} disabled={approved}/></label><label className="field"><span>Price We Choose to Quote</span><input name="proposed_sell_price" type="number" step="0.01" defaultValue={num(e.proposed_sell_price)} disabled={approved}/></label></div><label className="field"><span>Estimate Stage</span><select name="status" defaultValue={e.status} disabled={approved}><option value="draft">Still Building Price</option><option value="ready">Ready to Send</option><option value="declined">Lost / Declined</option>{e.status==='accepted'&&<option value="accepted">Customer Accepted</option>}</select></label>{!approved&&<button className="button secondary">Save Pricing</button>}</form></div></div></div>{!approved&&<details className="controls-disclosure section"><summary>Add Labor / Material / Equipment / Sub Cost</summary><div className="controls-body"><form action={addEstimateItem} className="form"><input type="hidden" name="estimate_id" value={e.id}/><div className="grid grid2"><label className="field"><span>Scope Area</span><select name="section_id" defaultValue=""><option value="">Unassigned</option>{secs.map((sec:any)=><option key={sec.id} value={sec.id}>{sec.name}</option>)}</select></label><label className="field"><span>What Kind of Cost?</span><select name="item_type" defaultValue="material"><option value="labor">Labor</option><option value="material">Material</option><option value="equipment">Equipment</option><option value="subcontractor">Subcontractor</option><option value="other">Other</option></select></label></div><div className="grid grid2"><label className="field"><span>Cost Code</span><select name="cost_code_id" defaultValue=""><option value="">Labor / none</option>{(codes||[]).map((c:any)=><option key={c.id} value={c.id}>{c.code} — {c.name}</option>)}</select></label><label className="field"><span>Saved Cost Item</span><select name="catalog_item_id" defaultValue=""><option value="">Custom</option>{(catalog||[]).map((c:any)=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label></div><label className="field"><span>Description</span><input name="description" required placeholder="Form walls / 4000 PSI concrete / #4 rebar / pump..."/></label><div className="grid grid2"><label className="field"><span>Worker (labor only)</span><select name="crew_member_id" defaultValue=""><option value="">Not labor / generic labor</option>{(crew||[]).map((c:any)=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label className="field"><span>L&I Work Class (labor only)</span><select name="risk_class_code" defaultValue="0217-01"><option value="">Owner / not applicable</option>{(risk||[]).map((r:any)=><option key={r.code} value={r.code}>{r.code} — {r.name}</option>)}</select></label></div><div className="grid grid2"><label className="field"><span>Labor Operation</span><select name="labor_task" defaultValue="Formwork"><option>Formwork</option><option>Rebar</option><option>Placement</option><option>Finishing</option><option>Strip</option><option>Cleanup</option><option>Layout</option><option>General</option></select></label><label className="field"><span>Labor Hours</span><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:8}}><input name="regular_hours" type="number" step="0.25" min="0" placeholder="Regular"/><input name="overtime_hours" type="number" step="0.25" min="0" placeholder="OT"/></div></label></div><div className="grid grid2"><label className="field"><span>Quantity</span><input name="quantity" type="number" step="0.01" min="0" defaultValue="1"/></label><label className="field"><span>Unit</span><select name="unit" defaultValue="LS">{units.map(u=><option key={u}>{u}</option>)}</select></label></div><label className="field"><span>Unit Cost</span><input name="unit_cost" type="number" step="0.01" min="0" defaultValue="0"/></label><button className="button">Add Cost</button></form></div></details>}<details className="controls-disclosure section"><summary>Show Estimate Cost Detail ({its.length})</summary><div className="controls-body"><div className="list">{its.length===0?<div className="meta">No cost lines yet.</div>:its.map((i:any)=><div className="row" key={i.id}><div><div className="title">{i.description}</div><div className="meta">{i.item_type} · {i.item_type==='labor'?`${num(i.regular_hours)+num(i.overtime_hours)} hr`:`${num(i.quantity).toFixed(2)} ${i.unit} × ${money(i.unit_cost)}`}{i.risk_class_code?` · ${i.risk_class_code}`:''}</div></div><strong>{money(i.direct_cost)}</strong></div>)}</div></div></details><div className="action-row section">{!approved&&<Link className="button secondary" href="/proposals">Create / Send Proposal</Link>}{!approved&&<form action={approveEstimateToBudget}><input type="hidden" name="estimate_id" value={e.id}/><button className="button" disabled={!e.project_id}>{e.status==='accepted'?'Award Job → Freeze Budget':'Approve → Freeze Project Budget'}</button></form>}{!approved&&!e.project_id&&<div className="meta">Link this estimate to a project before freezing the budget.</div>}</div>{approved&&<div className="alert success section"><strong>Project budget frozen.</strong> {e.project_id&&budgeted.has(e.project_id)?'This is now the original estimate-vs-actual baseline for the job.':'Budget link is being checked.'}</div>}</section></article>})}</div></section></div></AppShell>}
