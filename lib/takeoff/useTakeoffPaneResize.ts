'use client';

/**
 * Retained temporarily for import compatibility with the Quantity Worksheet.
 * Direction A uses stable-width, collapse-only side panes; horizontal pane
 * resizing is intentionally disabled.
 */
export function useTakeoffPaneResize() {}
